# Build output
/c2-bin

# Go
*.test
*.out

# OS
.DS_Store

# Web build (Phase 3)
web/node_modules
web/dist
/bin
# Note: internal/webdev/assets/ IS committed — it is the source-of-truth
# for `go:embed` so a clean clone can `go build` without Node installed.
