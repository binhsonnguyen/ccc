# Build output
/c3-bin
/c3-server

# Go
*.test
*.out

# OS
.DS_Store

# Claude Code local config (do not push)
.claude/

# Web build (Phase 3)
web/node_modules
web/dist
/bin

# Review agents drop screenshots here while inspecting the running app
.review/
.review-*
review-*.png
.tmp-*
# Note: internal/webdev/assets/ IS committed — it is the source-of-truth
# for `go:embed` so a clean clone can `go build` without Node installed.
